#include "raylib.h"
#include "clock.h"

/*
void StartGameClock(Timer *gameClock, double lifetime){
  gameClock->startTime = GetTime();
  gameClock->gameRunningTime = lifetime;
}

bool StopClock(Timer *gameClock){
  return GetTime() - gameClock.startTime >= gameClock.lifeTime;
}

double ElapsedGameClock(Timer timer){
  return GetTime() - gameClock.startTime;
}
*/

float gameClockRunning;

void StartGameClock(){
  gameClockRunning =
}
